import React from 'react';
import PropTypes from 'prop-types';

export default class CountryAttribute extends React.Component {
	render(){
		const props = this.props

		return (
			<div>
				country
			</div>
		)
	}
}

CountryAttribute.propTypes = {
	
};
