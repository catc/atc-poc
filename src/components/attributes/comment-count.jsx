import React from 'react';
import PropTypes from 'prop-types';

export default class CommentCountAttribute extends React.Component {
	render(){
		const props = this.props

		return (
			<div>
				comment-count
			</div>
		)
	}
}

CommentCountAttribute.propTypes = {
	
};
